<h2 class="text-lg font-semibold text-gray-800 mt-6 mb-4">
    {{ __('Completed documents') }}
</h2>
<ul class="list-disc pl-5 text-evisablack mb-8">
    <li>Mr. Stan Matrosov</li>
    <li>Mr. Oleg Matrosov</li>
</ul>


@php /*
<div>
    <h5>{{ __('Completed Documents') }}</h5>
    <p>{{ __('View all applicants completed documents') }}</p>
</div>

@if(count($order->certificates) > 0)
    <a href="{{ route('web.account.order.documents', $order->id) }}">
        <button class="btn btn-outline-primary">
            {{ __('View documents') }}
        </button>
    </a>
@endif
*/ @endphp